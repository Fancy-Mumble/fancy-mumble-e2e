{
  "name": "fancy-mumble-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end (Selenium + tauri-driver) tests driving the FancyMumble desktop client against a real Fancy Mumble server.",
  "type": "module",
  "scripts": {
    "test:e2e": "node --import tsx scripts/e2e.mts",
    "typecheck": "tsc --noEmit",
    "e2e": "node --import tsx scripts/e2e.mts",
    "e2e:private": "node --import tsx scripts/e2e.mts --private",
    "test:e2e:raw": "node --import tsx --test --test-concurrency=1 \"src/tests/**/*.test.ts\""
  },
  "dependencies": {
    "selenium-webdriver": "^4.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/selenium-webdriver": "^4.1.28",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
